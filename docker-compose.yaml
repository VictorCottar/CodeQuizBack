services:
  app:
    image: victorcottar/backendcodequiz:1.1
    container_name: backendcodequiz
    working_dir: /app
    #volumes:
    #  - .:/app
    #  - /app/node_modules
    ports:
      - "3000:3000"
    env_file:
      - .env